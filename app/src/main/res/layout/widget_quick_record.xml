<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="12dp">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_mic"
            tools:ignore="ContentDescription" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:text="@string/app_name"
            android:textColor="#333333"
            android:textSize="14sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Status -->
    <TextView
        android:id="@+id/tv_widget_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text="@string/tap_to_start"
        android:textColor="#666666"
        android:textSize="12sp" />

    <!-- Botões -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:gravity="center"
        android:orientation="horizontal">

        <!-- Botão Gravar -->
        <ImageButton
            android:id="@+id/btn_widget_record"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:background="@drawable/widget_record_button"
            android:contentDescription="@string/record"
            android:padding="12dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_mic_white"
            tools:ignore="TouchTargetSizeCheck" />

        <!-- Botão Ideia Rápida -->
        <ImageButton
            android:id="@+id/btn_widget_quick_idea"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginStart="24dp"
            android:background="@drawable/widget_idea_button"
            android:contentDescription="@string/quick_idea"
            android:padding="8dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_lightbulb"
            tools:ignore="TouchTargetSizeCheck" />
    </LinearLayout>

</LinearLayout>
